# Core
pandas>=2.0.0
streamlit>=1.30.0
streamlit-autorefresh>=1.0.1

# LangChain stack
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-google-genai>=2.0.0
google-generativeai>=0.8.0

# Vector DB + Embeddings
# chromadb 0.6+ uses pydantic v2 natively — required for Python 3.12+/3.14+
chromadb>=0.6.0
sentence-transformers>=3.0.0

# PyTorch CPU-only (Streamlit Cloud is CPU; avoids downloading 2 GB CUDA wheel)
torch
torchvision
torchaudio
--extra-index-url https://download.pytorch.org/whl/cpu

# Packet capture (live mode only; import is wrapped in try/except)
scapy>=2.5.0