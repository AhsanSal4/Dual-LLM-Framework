# Core
pandas>=2.0.0
streamlit>=1.30.0
streamlit-autorefresh>=1.0.1

# LangChain stack
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-google-genai>=2.0.0
google-genai>=1.0.0

# pydantic v2 required by chromadb 0.6+
pydantic>=2.0.0

# Vector DB — FAISS has no pydantic dependency, works on all Python versions
faiss-cpu>=1.7.0
sentence-transformers>=3.0.0
langchain-huggingface>=0.1.0

# PyTorch CPU-only
torch
torchvision
torchaudio

# Packet capture (live mode only; import is wrapped in try/except)
scapy>=2.5.0